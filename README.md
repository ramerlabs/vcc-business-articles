# VCC Business — Articles

AI-generated articles auto-committed daily by the n8n workflow `github-ai-article-daily.json`.

Files live in `articles/` and are named `YYYY-MM-DD-HHMMSS-slug.md` (Markdown + YAML front matter).
New runs never overwrite existing files.